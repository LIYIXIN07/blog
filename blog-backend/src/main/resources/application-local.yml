# 本地 Docker MySQL（3305）专用
# 启动：SPRING_PROFILES_ACTIVE=local
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3305/blog?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.blog: DEBUG
